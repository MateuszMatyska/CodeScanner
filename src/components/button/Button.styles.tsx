import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  btn: {
    width: '100%',
    borderRadius: 20,
  },
  title: {
    margin: 16,
    padding: 10,
    textAlign: 'center',
    fontSize: 24,
  },
});
